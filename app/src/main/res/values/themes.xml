<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme (Compose handles actual theming) -->
    <style name="Theme.PhoneLookup" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/dark_background</item>
        <item name="android:statusBarColor">@color/dark_background</item>
        <item name="android:navigationBarColor">@color/dark_background</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Splash screen theme for instant cold-start appearance -->
    <style name="Theme.PhoneLookup.Splash" parent="Theme.PhoneLookup">
        <item name="android:windowBackground">@color/dark_background</item>
    </style>
</resources>
